# AGENTS.md

本文件适用于仓库根目录及全部子目录，是人类开发者和 Codex 在本项目中的长期协作约定。

最后更新：2026-08-05

## 1. 项目目标

融资工作台服务于资金运营与融资项目团队，统一管理：

- 存续负债余额、品种结构和到期安排；
- 不同负债品种的 SOP 模板、项目、人员和任务节点；
- 可配置的节点邮件提醒；
- 现有借入资金 Excel 台账的 SQLite 导入和核对。

优先保证财务口径可追溯、操作路径清晰和桌面端信息密度。不要为了视觉效果牺牲数据可读性或引入未经核实的业务结论。

## 2. 技术栈与运行方式

- 包管理：pnpm
- 全栈框架：SvelteKit / Svelte 5
- UI：Tailwind CSS 4 + DaisyUI 5 + Lucide
- 数据库：SQLite / better-sqlite3
- Excel：SheetJS `xlsx`
- 邮件：Resend
- 部署适配：`@sveltejs/adapter-node`

常用命令：

```bash
pnpm install
pnpm db:init
pnpm db:import
pnpm reminders:send -- --dry-run
pnpm check
pnpm build
pnpm dev
```

## 3. 开始和结束每次会话

### 开始前

1. 完整阅读本文件和 `DESIGN.md`。
2. 查看工作区现状；已有改动默认属于用户，不要覆盖或回退。
3. 查看下方 TODO，确认本次工作对应的任务、优先级和验收标准。
4. 涉及 Excel 时先核对来源、日期列、单位和汇总口径；涉及 UI 时先核对字号与相对单位约束。

### 工作中

- 业务逻辑、数据库结构、运行命令或 UI 规范发生变化时，同一批修改必须同步更新相关文档。
- 发现新的真实缺口时立即加入 TODO；不要把猜测写成已确认问题。
- 完成一项 TODO 后，在本次会话内从 TODO 中删除，并写入“会话记录”；TODO 只保留尚未完成的事项。
- 对财务数据区分“数据错误”“来源缺失”和“口径不明确”，不得为了填满页面而编造数值。

### 交付前

1. 至少运行 `pnpm check` 和 `pnpm build`。
2. 数据导入变化还需运行 `pnpm db:import`，验证总额、品种余额和重复导入幂等性。
3. 提醒变化至少运行一次 `pnpm reminders:send -- --dry-run`。
4. 实时更新本文件的 TODO、最后更新时间和“会话记录”。
5. UI 变化同时更新 `DESIGN.md` 的规范或会话记录。

## 4. 目录与职责

- `src/routes/`：页面、页面服务端加载和表单动作。
- `src/lib/server/db.js`：SQLite 连接。
- `src/lib/server/schema.js`：表结构和索引。
- `src/lib/server/seed.js`：可重复执行的基础人员、SOP、项目和提醒种子。
- `src/lib/server/excel-import.js`：Excel 解析、明细幂等写入和余额快照核对。
- `src/lib/server/queries.js`：Dashboard、甘特图和设置页查询。
- `src/lib/server/reminders.js`：提醒计算、去重、Resend 发送和发送日志。
- `src/routes/workbench/`：融资日历、负债额度管理和发行试算。
- `src/routes/debts/[id]/`：单笔负债与来源单元格追溯。
- `scripts/`：数据库、导入和提醒 CLI。
- `data/`：原始 Excel；不得静默改写源文件。
- `database/`：本地 SQLite 数据库。
- `DESIGN.md`：人工维护的 UI 规范与偏好，是前端实现的直接约束。
- `design-system/default/MASTER.md`：生成式设计建议，仅作参考；与 `DESIGN.md` 冲突时以 `DESIGN.md` 为准。

## 5. 财务数据硬约束

- 生产数据只保留结构化业务字段；原始台账、工作簿导出、SQLite、JSON 和临时导入产物不得提交。
- 导入校验需验证字段覆盖、单位转换、总额与明细勾稽、数量、幂等性和外键完整性；具体结果只保留在本地运行环境。
- Excel 仅由本地维护脚本解析，解析变化先使用未提交的本地样本或测试夹具验证。

## 6. 前端与交互硬约束

完整规范见 `DESIGN.md`，以下为不可违背的摘要：

- 普通文字为 `1rem`。
- 可见小字不得小于 `0.75rem`。
- 字号禁止新增 `px`；标题优先使用 `clamp()`、`rem` 和 `vw`。
- 布局、间距、尺寸和断点优先使用 `%`、`rem`、`em`、`vw`、`vh`、`dvh`、Grid 和 Flex。
- `px` 仅用于 1–2px 细边框、焦点线、必要的 SVG 描边等不可替代场景。
- 桌面端保持高信息密度，但不得通过缩小字号实现。
- 使用 Lucide 图标，不使用 emoji 充当结构性图标。
- 交互必须有键盘焦点、可访问名称和 150–300ms 状态反馈。
- 必须支持 `prefers-reduced-motion`，移动端不得产生页面级横向滚动。
- 页面标题和模块标题下方不写没有新增业务信息的说明性小字；能由标题、字段或结构直接表达的内容直接删除，不用“自适应”等界面实现词填充版面。

## 7. 数据库与服务端约定

- 所有写操作使用参数化 SQL。
- 新表和新字段放入 `createSchema()`，迁移必须可重复运行。
- 外键保持开启；删除行为必须显式定义。
- 表单动作必须进行服务端必填、类型、范围和文件大小校验。
- 上传文件只进入明确的临时路径，导入结束后清理。
- 密钥只从环境变量读取；不得写入仓库、SQLite 或日志。
- Resend 未配置时只生成 `pending` 记录；不得假装发送成功。
- 提醒以 `(rule_id, target_id, delivery_date)` 去重。

## 8. 代码风格与变更纪律

- 优先小而明确的函数，避免在 Svelte 组件中堆积数据库逻辑。
- 服务端模块保留在 `$lib/server`，不得进入客户端 bundle。
- 重复映射和状态翻译应抽成共享函数或数据结构。
- 不做无关重构，不覆盖用户已有修改。
- 页面展示的真实数值必须能追溯到数据库或明确标注为演示数据。
- 新命令、新环境变量、新数据口径或新页面必须同步更新 `README.md`、本文件或 `DESIGN.md`。

## 9. 验收门槛

- `pnpm check`：0 error、0 warning。
- `pnpm build`：成功。
- 核心路由 `/`、`/workbench`、`/debts/[id]`、`/projects`、`/sop`、`/settings`、`/data`、`/people` 可访问。
- Excel 导入能够重复执行且汇总保持一致。
- 新增表单具有失败反馈，不得静默失败。
- 新增可见文字满足 `1rem / 0.75rem` 字号规则。
- 新增布局不得以大量固定 `px` 值实现。

## 10. 当前 TODO

以下仅保留未完成事项，按当前依赖关系重新排序。

### P1 — 质量保障

- [ ] 增加 Excel 解析、余额断言、提醒频率和项目建档的单元测试。
- [ ] 增加 `/`、`/projects`、`/sop`、`/settings`、`/data`、`/people` 核心路由与关键表单的端到端测试。
- [ ] 补充大字号/200% 缩放模式的视觉回归；375px、768px、1024px 和 1440px 已于 2026-08-04 检查。
- [ ] 复核字号放大后的 Dashboard、甘特图和设置页信息密度，必要时通过折叠、分组或横向局部滚动优化，而不是缩小文字。

### P2 — 查询与下钻

- [ ] 将筛选状态同步到 URL，支持刷新、返回和分享后保持。
- [ ] 增加负债明细表、排序、导出及从图表下钻能力。

### P2 — 发行试算

- [ ] 建立可按期间维护的财务指标数据表，接入 LCR、NSFR、资产负债率和长短期负债比试算。
- [ ] 将拟发行品种、起息日、规模和期限纳入到期当月集中度预估。

### P2 — 样式维护

- [ ] 统一消除组件中的重复颜色值，迁移到语义化 CSS token。

