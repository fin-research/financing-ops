# 融资工作台 UI 规范与偏好

最后更新：2026-08-21

本文件是融资工作台前端设计的人工维护基准。`design-system/default/MASTER.md` 可以提供灵感，但发生冲突时以本文件和用户最新要求为准。

## 1. 设计目标

- 专业、克制、可信，符合资金运营和融资项目管理场景。
- 桌面端高信息密度，但不能靠小字体、低对比度或拥挤点击区域换取密度。
- 优先展示结论、风险、日期和责任人，装饰元素从简。
- 仪表盘聚合监管指标、存量结构、到期分布、推进项目、月度发行、负债额度、发行试算、融资日历与待办提醒；项目页以甘特图为核心。
- 所有关键视图可按负债品种和人员筛选。
- 同一筛选维度支持多选；空选择表示全部，已选数量必须在收起状态下仍可识别。

## 2. 字体规范

根字号使用浏览器默认值：

```css
html {
  font-size: 100%;
}
```

字号层级：

| Token | 建议值 | 用途 |
|---|---:|---|
| `--font-body` | `1rem` | 正文、导航、按钮、输入、表格主要内容 |
| `--font-small` | `0.75rem` | 辅助说明、时间、单位、标签；可见文字的绝对下限 |
| `--font-title-sm` | `1.125rem` | 卡片或弹窗标题 |
| `--font-title-md` | `1.375rem` | 页面内主要模块标题 |
| `--font-title-lg` | `clamp(1.5rem, 2vw, 1.875rem)` | 页面标题 |
| `--font-kpi` | `clamp(1.5rem, 2.2vw, 2rem)` | 核心财务指标 |

硬性要求：

- 普通文字必须是 `1rem`。
- 可见小字不得小于 `0.75rem`。
- 禁止新增 `font-size: Npx`。
- `font-size: 0` 只允许用于移动端把带图标按钮切换成图标模式，此时按钮必须保留可访问名称。
- 正文行高使用 `1.5–1.7`，密集表格和标签不得低于 `1.35`。
- 金额、利率、日期和进度数字使用 tabular figures，避免数值变化引起抖动。
- 字重以 400/500/600/700 为主，不用极细字重承载重要数据。

字体栈：

```css
font-family:
  Inter, "PingFang SC", "Microsoft YaHei",
  system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
```

优先使用系统字体，避免额外字体网络请求影响内部工作台首屏。

## 3. 相对单位规范

### 优先级

1. `%`、Flex、Grid：区域分配、列宽和图表占比。
2. `rem`：全局间距、字号、圆角、控件高度、固定导航尺度。
3. `em`：与组件自身字号绑定的图标间距、徽标和行内元素。
4. `vw` / `vh` / `dvh`：流式标题、视口高度、宽屏自适应。
5. `clamp()`、`min()`、`max()`、`minmax()`：为相对值设置合理边界。

默认禁止使用固定 `px`。以下情况允许保留：

- `1px` 或 `2px` 的边框、分隔线、焦点线；
- SVG 图标描边或浏览器需要的像素级细节；
- 无法用 CSS 相对单位表达且已有明确测试依据的第三方组件接口。

允许例外时必须局部、可解释，不得用固定像素搭建整页布局。

### 推荐写法

```css
.page {
  width: min(100%, 90rem);
  padding-inline: clamp(1rem, 2.5vw, 2rem);
}

.dashboard-grid {
  display: grid;
  grid-template-columns: minmax(0, 1.4fr) minmax(18rem, 0.8fr);
  gap: 1rem;
}

.page-title {
  font-size: clamp(1.5rem, 2vw, 1.875rem);
}
```

避免：

```css
.page {
  width: 1440px;
  padding: 28px;
}
```

## 4. 间距与尺寸

使用 0.25rem 基础节奏：

| Token | 值 | 用途 |
|---|---:|---|
| `--space-1` | `0.25rem` | 紧密行内间距 |
| `--space-2` | `0.5rem` | 图标与文字 |
| `--space-3` | `0.75rem` | 控件内部间距 |
| `--space-4` | `1rem` | 卡片和网格标准间距 |
| `--space-5` | `1.25rem` | 模块内部大间距 |
| `--space-6` | `1.5rem` | 页面区块间距 |
| `--space-8` | `2rem` | 页面级留白 |

控件要求：

- 桌面按钮和输入框最小高度 `2.75rem`。
- 触控场景交互区域不得小于 `2.75rem × 2.75rem`。
- 卡片圆角建议 `0.5rem–0.75rem`。
- 弹窗宽度使用 `min(92vw, 36rem)`。
- 主内容宽度使用 `min()` 或 `max-width` 的 `rem` 上限，不锁死固定像素。

## 5. 布局规范

### 应用框架

- 宽屏使用固定语义尺度的侧栏和弹性内容区。
- 内容区必须使用 `minmax(0, 1fr)`，防止图表或甘特图撑破页面。
- 顶栏可 sticky，但主内容必须保留相应空间。
- 页面标题统一放在顶栏，内容区不重复放置 `page-heading`；二级详情页由顶栏显示当前对象名称。
- 顶栏标题右侧使用带语义色的文字 chip 显示当前角色；账号区只显示单行用户名称，不重复角色和登录名。
- 小屏切换为顶部品牌栏和底部导航，页面级不得横向滚动。
- 桌面侧栏按“工作空间”和“数据管理”分组；SOP 管理使用 `/sop`，个人设置使用 `/settings`，数据后台和人员与权限继续使用独立路由，不使用同页锚点承担主导航。
- 登录页使用单列聚焦布局，不显示业务侧栏；错误信息就地呈现并使用 `role="alert"`。

### Dashboard

- 第一视觉层：9 个融资指标按 3×3 独立卡片排列，每卡使用不同语义色的大图标；小标题和比较文字均为 `1rem`，数值正负同时用符号和颜色区分。
- 第二视觉层：存量负债结构与到期分布平齐放在同一行。
- 第三视觉层：推进中融资项目和月度发行统计左右并排；未来 30 天到期仅保留指标卡，不再列示逐笔明细表。
- 第四视觉层：负债额度管理与发行试算、融资日历与待办提醒按同一两列网格排布，左宽右窄、间距与上方行一致。
- 待办预警集中在仪表盘“待办与提醒”模块与全局滚动提醒条；项目内容区不放待办、预警和日历。
- 金额默认以亿元展示，同时保留明确单位和数据日期。
- 图表必须提供图例、直接数值或文本摘要；不能只靠颜色表达。
- 四项监管监控指标使用与其他指标一致的中性卡片；标题右侧状态灯按正常、超过监管上限 80%、超过监管上限依次显示绿、黄、红，卡片本身不随状态变色。下方先列监管上限再列业务数值，仅数字使用状态色。
- 页面标题和模块标题下不增加没有业务增量的说明性小字；标题、字段或图表已经表达的信息不重复说明，不出现“自适应”等实现词。

### 甘特图

- 项目信息、状态、时间轴保持三层清晰结构。
- 时间位置与宽度使用百分比。
- 桌面端允许甘特区域局部横向滚动，但不得造成整个页面横向滚动。
- 移动端优先切换为项目卡片，不强行压缩完整时间轴。
- 状态必须同时使用文字和颜色。

### 日历

- 网格使用相对列宽；融资日历默认仅周一至周五五列，悬停日历边缘浮现箭头可展开为周日开始的七列完整周。
- 日期、事件名称和风险层级均应可读。
- 事件过多时使用“更多”入口或抽屉，不继续缩小字体。
- 融资日历按每笔融资性负债完整展示，不设内部滚动条，允许日历随事件数量增高。
- 日历事件格式为“负债简称•事件摘要”，按品种颜色区分并可点击下钻。

## 6. 色彩与层级

语义色：

- 背景：冷灰白 `--canvas`
- 表面：白色 `--surface`
- 主文字：深灰蓝 `--ink`
- 次级文字：`--muted`
- 主操作：蓝色 `--blue`
- 正向：青绿 `--teal`
- 警示：橙色 `--orange`
- 风险：红色 `--red`
- 辅助分类：紫色 `--violet`

规则：

- 正文对比度至少 4.5:1。
- 风险、完成、等待等状态必须带文字或图标，不得只依赖颜色。
- 一页只保留一个最高视觉优先级的主操作。
- 主要新建操作使用固定在页面右下角的圆形 Lucide 加号按钮，必须有 `aria-label` 和 `title`；仪表盘不放刷新或导入快捷按钮。
- 阴影保持轻量；通过边框、背景和间距建立层级。
- 新增颜色优先扩展语义 token，不在组件中重复散落十六进制值。

## 7. 组件与交互

- 使用 Lucide 线性图标，保持同层级尺寸和描边一致。
- 图标按钮必须有 `aria-label`。
- 所有表单使用可见 label，不以 placeholder 代替。
- 原生 `dialog` 必须在视口中水平、垂直居中，并限制最大高度；内容超出时仅弹窗内部滚动。
- `:focus-visible` 必须清晰可见。
- hover、focus、pressed 反馈使用 150–300ms。
- 动画只使用 opacity/transform 等低成本属性，并支持 `prefers-reduced-motion`。
- 加载超过约 300ms 时显示 loading；提交期间禁用重复提交。
- 成功、失败信息使用 `aria-live` 或 `role="alert"`，并提供下一步操作。
- 当前登录账号和角色必须明确可见；退出登录使用带可访问名称的表单按钮，不以纯前端状态代替服务端会话。
- 顶栏提醒使用醒目的文字按钮和数字角标；展开后列示未来 7 天及已逾期的项目节点，并可从每条提醒直接进入对应项目。
- 点击顶栏账号进入 `/settings` 个人设置；头像、显示姓名、登录邮箱和密码均由当前用户自行维护，不再维护独立登录账户名；修改登录邮箱必须验证当前密码。
- 人员与权限页使用统一人员主档承接项目责任、系统角色与可选登录账号，不将账号另列为第二套人员；增删改操作必须带确认、提交状态和就地反馈。
- 当前角色在全局顶栏标题右侧用文字与颜色 chip 统一标示；经办默认筛选本人负责项目，复核与管理员默认显示全部。

## 8. 响应式断点

断点使用 `rem`，以浏览器默认 16px 根字号为基准：

- `90rem`：超宽桌面。
- `75rem`：标准桌面。
- `64rem`：窄桌面/横屏平板。
- `51.25rem`：平板与移动导航切换。
- `35rem`：小屏手机。
- `23.4375rem`：375px 等效验证宽度，仅用于测试，不建议堆叠特例。

每次重要 UI 变更至少检查：

- 375px 等效小屏；
- 768px 等效平板；
- 1024px 等效窄桌面；
- 1440px 等效宽桌面；
- 浏览器 200% 缩放或大字号模式；
- `prefers-reduced-motion: reduce`。

## 9. 数据展示偏好

- 真实财务数值优先于装饰性趋势。
- 无可靠数据时显示“暂无可靠数据”，不要使用无来源的百分比或 BP 变化。
- 同一页面金额小数位保持一致；汇总对账可保留 4 位，概览通常 1–2 位。
- 数据日期始终在筛选栏、卡片标题或图表说明附近展示。
- 筛选后 KPI、图表、日历和项目数量应使用同一筛选口径。
- 图表需有文本替代或清晰的 `aria-label`。

## 10. 当前偏好

- 中文企业级资金运营界面。
- 深色导航 + 浅色内容区。
- 自适应双列/多列桌面布局。
- 高密度但不牺牲 1rem 正文字号。
- 少装饰、轻阴影、清晰分隔。
- 重要风险集中呈现，普通状态降低视觉噪音。
- 优先通过折叠、分组、局部滚动和渐进披露解决拥挤，不通过缩小字体解决。

## 11. 会话记录

### 2026-08-21

- 应用固定使用 `/financing` path prefix；导航、表单、API 请求、登录重定向和会话 Cookie 均限制在该前缀内。
- 登录标识统一为人员主档中的唯一邮箱，删除账户名维护入口；个人设置支持验证当前密码后修改登录邮箱，并继续支持修改密码和退出其他设备会话。

### 2026-08-20

- 数据后台“导入完整性”模块增加仅管理员可见的“重新统计”按钮；按钮明确给出扫描中、成功和失败反馈。
- 日常页面只展示 Excel 导入时保存的统计快照；快照尚未生成时显示“待统计”，不在页面加载阶段自动扫描完整业务表。

### 2026-08-19

- 数据后台的 Excel 上传由全量暂存切换改为增量追加：浏览器本地解析后先只读比对业务键，日余额仅保留晚于线上最大日期的记录，最后一次原子提交新增数据；相同文件直接返回且不产生 D1 写入。
- 上传进度区分“核对线上基准日”“只读核对历史记录”和“原子写入新增日期与记录”，成功反馈显示新增负债、新增日期和实际写入业务行。
- 负债详情移除来源单元格追溯表达，改为“核心信息 + 品种专属字段/计划 + 结构化现金流”，查询直接联接主表和扩展表。
- 上传成功反馈同时显示基准日、汇总余额以及新增、更新、删除数量；失败或网络中断保留线上旧数据，并允许重新选择文件开始。

### 2026-08-06

- 工作台负债额度管理上移至筛选与融资日历之前；筛选模块以无边框紧凑变体嵌入融资日历标题栏右侧，移除月份 period-badge。
- 全局顶栏下方新增粘性滚动“提醒”播报条：双份轨道无缝循环、悬停/聚焦暂停、左右渐隐；减少动效偏好下退化为静态换行列表。
- 仪表盘三项净资本相关监管指标卡片采用紧凑规格（2.5rem 图标、0.75rem 标题、1.125–1.5rem 数值、收紧间距与卡片高度）。

### 2026-07-28

- 建立本设计规范，并明确其优先级高于生成式 `MASTER.md`。
- 将普通文字基线定为 `1rem`，可见小字下限定为 `0.75rem`。
- 规定布局优先使用 `% / rem / em / vw / vh / dvh`。
- 将现有样式中大多数大于 2px 的绝对长度等价迁移为 `rem`。
- 保留 1–2px 边框、焦点线等必要例外。
- 增加登录页、管理员/只读角色标识及退出入口；继续遵守 `1rem / 0.75rem` 字号和相对单位规则。
- 增加项目详情、SOP 节点编辑和提醒发送历史页，延续深色导航、浅色内容区与高密度卡片/表格层级。
- 人员、项目、SOP 与提醒表单统一使用页面内 loading、禁用提交、成功提示和可恢复错误提示。
- 首页日历和项目页未来 7 天支持任务、付息及到期三类事件，均以文字和语义色共同区分。
- 首页与项目页筛选支持负债品种、负责人和状态多选，空选择表示全部。
- 弹窗统一在视口中央显示并限制为弹窗内部滚动。
- 设置区拆分为 SOP 与提醒、Excel 数据、人员与权限三个独立页面，侧栏和移动导航均使用直接路由。
- 工作台事件视图与启用 SOP 保持一致；完整 Excel 现金流仍保留在数据层，未被 SOP 覆盖的品种不进入日历和未来事件。

### 2026-08-04

- 原总览页改为仪表盘，采用 3×3 指标、双列图表和左长右短明细布局。
- 新增工作台页，融资日历、额度管理和发行试算完整展示，不使用卡片内滚动。
- 筛选条抽成可复用的预设+品种多选组件，浮动/固定收益凭证在紧凑选项中显示为“浮收/固收”。
- 融资日历改为按每笔负债展示，不再仅受启用 SOP 品种限制；默认预设排除同业拆借和浮动收益凭证。
- 实际检查 375px、768px、1024px 和 1440px 布局，新增页面无页面级横向滚动。

### 2026-08-05

- 待办与预警迁入全局顶栏提醒列表，使用高强调文字按钮和数字角标；移除顶栏日期按钮，每条提醒直接下钻对应项目。
- SOP 管理路由改为 `/sop`，`/settings` 专用于个人设置；顶栏账号改为可点击入口，并在当时支持头像、姓名、邮箱、登录用户名和密码维护；登录用户名入口已于 2026-08-21 移除。
- 取消全部内容区 `page-heading`，页面标题统一由顶栏承载；当前角色改为标题右侧的语义色文字 chip，账号区仅保留单行用户名称。
- 人员、项目和 SOP 的主要新建入口改为页面右下角圆形按钮；删除仪表盘刷新/导入快捷入口和数据后台“重新导入基准表”入口。
- 人员与权限页改为统一身份表和三角色概览，人员、角色、账号及状态在同一行关联展示；项目进度页增加醒目的当前角色与默认范围提示。
- 四项监管监控指标改为中性卡片加右上状态灯，增加 80% 黄色预警档；下方按“监管上限—业务数值”顺序展示且仅数字着色。
- 到期分布改为带纵轴刻度、直接数值和红橙蓝高低层次的六个月柱状图；删除页面标题及仪表盘模块标题下无业务增量的说明小字。
- 删除仪表盘下方的未来 30 天到期负债明细表，将推进中融资项目与月度发行统计调整为并排卡片。
- 仪表盘指标恢复为 3×3 独立彩色卡片，使用大号 Lucide 图标、1rem 加粗小标题和 1rem 比较文字；变化值同时使用正负号与红绿语义色。
- Excel 数据页更名为数据后台并承接监管指标计算参数；SOP 与提醒页更名为 SOP 管理，不再展示负债品种二级分类模块。
- 工作台日历、额度表和发行试算统一为深蓝灰文字、单一蓝色主强调和轻边框，额度表增加文本值与使用进度的双重表达。
- 筛选条采用蓝色状态边和浅色背景，同时保留清晰焦点、150–300ms 反馈与 `prefers-reduced-motion` 支持。
