# 融资工作台 UI/UX 规范

本文件是全部页面的当前设计基准。`design-system/default/MASTER.md` 仅可提供参考；发生冲突时以本文件和用户最新要求为准。

## Design Principles

- 专业、克制、可信，优先展示结论、风险、日期、金额和责任人。
- 桌面端保持高信息密度，但不靠小字体、低对比度或缩小点击区域换取密度。
- 深色导航配浅色内容区，使用轻边框、低阴影和明确的语义色。
- 页面真实数值必须可追溯；无可靠数据时显示“暂无可靠数据”，不放演示值。
- 页面标题由全局顶栏承载，内容区不重复标题；标题下不写没有业务增量的说明小字。

## Layout

- 宽屏使用固定语义尺度侧栏和 `minmax(0, 1fr)` 弹性内容区。
- 主内容使用 `%`、Grid、Flex、`min()`、`max()` 和 `clamp()`；不以固定像素搭整页布局。
- 页面间距使用 0.25rem 节奏：`0.25 / 0.5 / 0.75 / 1 / 1.25 / 1.5 / 2rem`。
- 标准卡片圆角 `0.5–0.75rem`，弹窗宽度 `min(92vw, 36rem)`。
- 小屏切换为顶部品牌栏和底部导航；页面级不得横向滚动，表格和甘特图只允许组件内部滚动。

允许 `px` 的范围仅限 1–2px 边框、焦点线、SVG 描边或有测试依据的第三方接口。其他尺寸优先使用相对单位。

## Typography

- 根字号使用浏览器默认 `100%`。
- 正文、导航、按钮、输入和主要表格文字使用 `1rem`。
- 可见小字下限 `0.75rem`，只用于时间、单位、标签等辅助内容。
- 模块标题从 `1.125rem` 起；页面标题与 KPI 使用 `clamp()`。
- 禁止新增 `font-size: Npx`；数字使用 tabular figures。
- 正文行高 `1.5–1.7`，密集表格和标签不得低于 `1.35`。
- 使用系统字体栈与中文系统字体，不增加外部字体请求。

## Color, Border and Shadow

- 颜色通过全局语义 token 使用：冷灰画布、白色表面、深灰蓝正文、蓝色主操作、青绿正向、橙色警示、红色风险、紫色辅助分类。
- 正文对比度至少 4.5:1。状态必须同时用文字、图标或符号表达，不只依赖颜色。
- 一页只保留一个最高视觉优先级的主操作。
- 阴影轻量，通过背景、边框和间距建立层级；不使用无业务含义的渐变。
- 新颜色先扩展语义 token，不在组件中散落重复十六进制值。

## Components

### Button and Icon

- 桌面按钮和输入框最小高度 `2.75rem`；触控区域不小于 `2.75rem × 2.75rem`。
- 结构图标统一使用 Lucide，不用 emoji。
- 图标按钮必须有 `aria-label`；主要新建动作使用右下角圆形加号并提供 `title`。
- hover、focus、pressed 反馈保持 150–300ms，并支持 `prefers-reduced-motion`。

### Form

- 所有字段使用可见 label，不以 placeholder 代替。
- 新建表单仅在成功后清空；校验或提交失败保留输入并就地提示。
- 项目、任务、SOP 模板和节点的普通字段使用约 650ms 防抖、串行自动保存；日期、下拉和失焦可立即提交。
- 自动保存用 `aria-live="polite"` 短暂提示“已保存”，不抢焦点。旧响应不得覆盖更新的输入。
- 创建、删除、启停、密码、账号权限和数据后台逐行编辑继续显式提交。

### Table

- 表头清晰、行高一致、名称左对齐、数字右对齐。
- 计算列和主键展示为只读；编辑反馈就地出现。
- 窄屏只允许表格容器横向滚动，不得撑宽整个页面。

### Modal and Destructive Action

- 原生 `dialog` 必须在视口水平、垂直居中，并限制最大高度；溢出仅在弹窗内部滚动。
- 删除等危险操作必须确认。提交期间禁用重复操作，失败后保留上下文和可恢复路径。

### Loading, Error and Empty State

- 操作超过约 300ms 显示加载状态。
- 错误使用 `role="alert"`；成功反馈使用 `aria-live`。
- 文案直接说明状态和下一步，不写装饰性或实现性描述。

## Page Contracts

### Dashboard

- 第一层为两行三列（2 行、3 列）融资指标；前五格展示核心指标，第六格为四项监管指标 2×2 组合；每张卡保留名称、主值和有决策意义的比较值。
- 第二层为存量负债结构与到期分布；第三层为推进项目与月度发行；第四层为额度/试算与日历/提醒。
- 四项监管指标使用中性卡片和绿/黄/红状态灯，依次表示正常、超过上限 80%、超过上限；卡片本身不随状态变色。
- 金额默认以亿元展示并明确单位和数据日期。图表必须提供图例、数值或文字摘要。

### Project and Gantt

- 项目信息、状态和时间轴保持清晰层级。
- 时间范围由上海时区当天、计划簿记日和节点日期动态覆盖，并按完整月份扩展。
- 月、季刻度、项目条、节点条和“今天”线使用同一百分比时间轴；禁止硬编码年月或位置。
- 移动端优先使用项目卡片，不强行压缩完整甘特图。

### SOP

- 节点使用左侧手柄拖拽排序，鼠标、触控和键盘共用同一原子服务端动作。
- 新增节点只通过右下角固定加号打开居中模态框，不保留页面常驻新增表单。

### Calendar and Reminders

- 融资日历默认周一至周五，可展开为周日开始的七列完整周。
- 事件按“负债简称•事件摘要”展示并可下钻；事件过多使用“更多”入口，不缩小字号。
- 顶栏提醒展示未来 7 天与逾期节点，每条可直接进入对应项目。

### Liability Weekly Report

- 负债周报视觉样式沿用安装包 HTML 模板（深蓝渐变页眉、A4 白色卡片、模板表格与状态色）；主内容最大宽度为 `1080px`，屏幕与打印共用六个 A4 页面 DOM，依次承载“第一+第二部分 / 第三 / 第四 / 第五 / 第六 / 第七”，每页固定展示公司名和“第 N 页 · 共 6 页”页脚。打印时保持 `1080px × 1527px` 虚拟画布，以 `210mm / 1080px` 的固定 `zoom` 等比缩放到 A4，不触发窄纸宽响应式重排；不套用融资工作台的通用页面 token 或共享指标卡样式。
- 负债周报以余额快照作为主动负债余额和结构的事实来源；明细余额未与快照勾稽时，所有模块仍保留，相关数值旁明确标注“数据缺失/待核对”，不得用整块隐藏掩盖缺口。
- 报告首页页眉只展示生产数据对应的报表日期和编制部门，不设置独立数据口径条；滞后、勾稽和来源缺口只在用户生成周报后通过全局系统消息提醒，不在报告正文常驻展示，也不能用安装包模板中的静态值或本地 Excel 产物替代生产数据。
- 周报全部业务图表统一使用 ECharts 组件，不得在页面或组件中手绘 SVG、Canvas、CSS 饼图或仪表盘；五个监管指标复用市场点评仪表盘的细弧、指针、锚点和阴影构造，再按监管预警与上限分段着色；柱体、饼图色块和进度色块只用纯色，禁止开启 ECharts decal 底纹；图例、tooltip、单位、空状态、响应式和减少动效必须完整。
- 公司融资余额趋势以余额快照为事实来源，余额使用面积图，横轴只显示年度刻度并标注年度节点；近一年公司债发行趋势固定拆分短融、3年/5年公募债、3年/5年次级债，各品种加权发行利率与柱体同色且只标注最后一个有效值，图例不重复列示利率系列。负债到期堆叠柱的最大合计标签使用红色。可比券商年度发行按总额降序，证券公司债按原始期限拆为公募债和短期公司债，堆积条形内部标注分项数值，并将东方财富名称和合计标红。
- “近期负债发行与到期动态”的“本周”只汇总报告日所在周周一至报告日已经发生的发行、到期和付息事件，“下周”取下一自然周周一至周五；不把推进中的发行计划计入事件或金额，融资计划只在独立计划表展示。收益凭证发行日优先取认购日。
- “本周券商债券发行定价”与“本周券商债券申报动态”按业务顺序组成连续双栏内容流：发行定价置于左栏，申报动态紧随其后；申报记录较多时前半留在左栏、续表从右栏顶部开始并在标题标注“（续表）”，两栏顶部表格头部平齐；小屏恢复单栏顺序排布。
- 周报全部表格使用内容驱动的自动列宽并允许表头和长文本换行，不设置横向滚动条或固定最小表宽；额度使用进度图只占用所在单元格的自适应短宽度。可比券商图表和表格统一删除公司制法律后缀，展示券商简称。
- 可比券商周表按所选报告日所在周的周一至报告日取数：发行定价限证券公司债、证券公司次级债和证券公司短期融资券；申报动态由统一 Data API 固定证券业二级行业及指定申报品种读取 DM，并使用上游返回的项目、发行人、状态、品种、金额和更新日。
- 未来 30 天和年内到期核心指标统计全量已安排负债，包含尚未发行但已登记到期日的记录；仅未来 30 天到期明细排除同业拆借和浮动收益凭证。明细只列负债本金到期，不把债券独立付息现金流伪装为“负债到期”。
- 周报只能由用户显式点击生成；浏览器直接并行调用统一数据服务的 Choice CTR、分页 DM 券商债券申报和 Neon Data API 聚合 RPC，financing Worker 不代理外部请求，也不执行大范围报表 SQL。利率走势只读统一 Neon `public.edb`，由 dashboard 每日定时增量更新，页面访问和手动生成均不得请求 Choice EDB。`saveSnapshot` action 只校验三类返回数据并固化快照；Choice 或 DM 失败时对应模块留空并提示，不使用安装包导入数据回退。最终把可信数据库数据、Choice CTR、DM 申报、缺失模块和口径固化为现有 R2 桶 `eastmoney` 的 `liability-report/yyyy-mm-dd.json` 快照；同一日再次生成覆盖同一对象和数据库索引行。
- 国有行同业存单曲线直接读取 `public.edb` 已入库观测，不在周报读取层应用通用离群值过滤；上游已确认的异常点由 Dashboard 同步写入端精确剔除并从数据库删除。
- 顶栏使用“报告日”日历选择器，默认上海时区当天；报告页眉、全部统计日期和本周区间均随报告日联动。进入页面或切换日期时只自动读取该日已有 R2 快照；没有快照时展示空状态，不自动拉取 Neon、Choice 或 DM，只有点击“生成本期周报”才取数。缺少资产负债规模、`public.edb` 利率序列、信用利差、同业发行或项目预计利率时，保留业务模块，并在生成完成后通过全局系统消息写明具体缺口。

### Identity and Navigation

- 顶栏标题右侧以文字 chip 显示当前角色；账号区只显示单行姓名。
- 点击账号进入 `/settings`；个人可维护头像、显示姓名和密码，邮箱只读并由管理员维护。
- 人员与权限使用同一人员主档承接项目责任、业务角色和可选登录账号，不建立第二套人员列表。

## Responsive and Accessibility

- 主要断点使用 `90rem / 75rem / 64rem / 51.25rem / 35rem`；375px 等效宽度仅用于验证，不堆叠特例。
- 重要 UI 变化应覆盖手机、平板、窄桌面、宽桌面、200% 缩放和减少动效场景。
- 所有交互支持清晰的 `:focus-visible`、可访问名称和键盘操作。
- 筛选后的 KPI、图表、日历和项目数量使用同一筛选口径；空多选表示全部。

## Reuse Rules

- 修改前搜索同类页面、组件、样式 token、表单 action 和图标。
- 优先扩展已有组件，不创建重复 Button、Card、Modal、Table 或筛选控件。
- 业务组件只组合既有视觉语言，不为单页创建新主题、间距体系或按钮变体。

## Don't

- 不使用 emoji 替代产品图标。
- 不通过减小字号解决拥挤。
- 不用颜色作为唯一状态信号。
- 不在单页新增品牌色、渐变、任意阴影或固定宽屏布局。
- 不在成功后重置编辑表单或用 `invalidateAll` 刷新整页。
- 不在标题下重复字段、图表或实现已经表达的信息。
